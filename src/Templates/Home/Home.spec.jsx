describe('<Home />', () => {
  it('is a dummy test', () => {
    expect(1).toBe(1);
  });

  it('test two', () => {
    expect(2).toBe(2);
  });

  it('test three', () => {
    expect(3).toBe(3);
  });
});
